query tracks ($limit: Int!) {
    Tracks(limit: $limit) {
        tracks {
            id
            artist
            title
            version
            label
            image
            trackUrl
            buyUrl
            votes
            createdAt
            updatedAt
        }
    }
}
