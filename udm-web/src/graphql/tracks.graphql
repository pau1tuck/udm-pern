query tracks ($limit: Int!) {
    Tracks(limit: $limit) {
        tracks {
            id
            artist
            title
            version
            label
            image
            trackUrl
            votes
            createdAt
            updatedAt
        }
    }
}
